[www]
; The default pool only permits five simultaneous PHP requests, which is too
; restrictive for the public booking flow and owner dashboard.
pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 6
pm.max_requests = 500
